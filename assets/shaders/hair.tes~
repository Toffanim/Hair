#version 410 core
 
layout(isolines, equal_spacing) in;
uniform mat4 MVP;

 
void main()
{
    float u = gl_TessCoord.x;
    int iu = int(u);
    float v = gl_TessCoord.y;
    gl_Position = MVP * (gl_in[0].gl_Position + gl_TessCoord.x);
}
